// Pass this to extendTheme in components:
export const DatePickerThemeConfig = {
    parts: ["input", "header", "content"],
    baseStyle: {
        input: {
            size: "md"
        }
    },
    // The size styles for each part
    sizes: {},
    // The variant styles for each part
    variants: {},
    // The default `size` or `variant` values
    defaultProps: {},
}